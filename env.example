# Portainer environment configuration (example for manual setup)

# Portainer web UI port
PORTAINER_PORT=9000

# Absolute host path for Docker bind volume (ZFS mountpoint)
PORTAINER_DATA_VOLUME=/tank/docker/volumes/portainer/data

# ZFS-specific configuration (used by preflight.sh)
ZPOOL=tank
PORTAINER_DATASET_PATH=docker/volumes/portainer/data
PORTAINER_DATASET=tank/docker/volumes/portainer/data
